stages:
  - build
  - test
  - docker
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

# Build Frontend
build:frontend:
  stage: build
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd frontend
    - npm ci
    - npm run build
  artifacts:
    paths:
      - frontend/.next
      - frontend/out
    expire_in: 1 hour

# Build Backend Node
build:backend-node:
  stage: build
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-node
    - npm ci
  artifacts:
    paths:
      - backend-node/node_modules
    expire_in: 1 hour

# Build Backend .NET
build:backend-dotnet:
  stage: build
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-dotnet
    - dotnet restore
    - dotnet build --configuration Release --no-restore
  artifacts:
    paths:
      - backend-dotnet/bin
      - backend-dotnet/obj
    expire_in: 1 hour

# Build Backend Spring Boot
build:backend-springboot:
  stage: build
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-springboot
    - mvn clean package -DskipTests
  artifacts:
    paths:
      - backend-springboot/target/*.jar
    expire_in: 1 hour

# Test Backend Node
test:backend-node:
  stage: test
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-node
    - npm ci
    - echo "Tests OK"
  dependencies:
    - build:backend-node

# Test Backend .NET
test:backend-dotnet:
  stage: test
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-dotnet
    - dotnet test --no-restore --verbosity normal
  dependencies:
    - build:backend-dotnet

# Test Backend Spring Boot
test:backend-springboot:
  stage: test
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - cd backend-springboot
    - mvn test
  dependencies:
    - build:backend-springboot

# Docker Frontend
docker:frontend:
  stage: docker
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/frontend:latest -t $CI_REGISTRY_IMAGE/frontend:$CI_COMMIT_SHORT_SHA ./frontend
    - docker push $CI_REGISTRY_IMAGE/frontend:latest
    - docker push $CI_REGISTRY_IMAGE/frontend:$CI_COMMIT_SHORT_SHA
  dependencies:
    - build:frontend

# Docker Backend Node
docker:backend-node:
  stage: docker
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/backend-node:latest -t $CI_REGISTRY_IMAGE/backend-node:$CI_COMMIT_SHORT_SHA ./backend-node
    - docker push $CI_REGISTRY_IMAGE/backend-node:latest
    - docker push $CI_REGISTRY_IMAGE/backend-node:$CI_COMMIT_SHORT_SHA
  dependencies:
    - build:backend-node

# Docker Backend .NET
docker:backend-dotnet:
  stage: docker
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/backend-dotnet:latest -t $CI_REGISTRY_IMAGE/backend-dotnet:$CI_COMMIT_SHORT_SHA ./backend-dotnet
    - docker push $CI_REGISTRY_IMAGE/backend-dotnet:latest
    - docker push $CI_REGISTRY_IMAGE/backend-dotnet:$CI_COMMIT_SHORT_SHA
  dependencies:
    - build:backend-dotnet

# Docker Backend Spring Boot
docker:backend-springboot:
  stage: docker
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/backend-springboot:latest -t $CI_REGISTRY_IMAGE/backend-springboot:$CI_COMMIT_SHORT_SHA ./backend-springboot
    - docker push $CI_REGISTRY_IMAGE/backend-springboot:latest
    - docker push $CI_REGISTRY_IMAGE/backend-springboot:$CI_COMMIT_SHORT_SHA
  dependencies:
    - build:backend-springboot

# Deploy (Manuel)
deploy:production:
  stage: deploy
  tags:
    - windows
    - docker
    - efrei_project
  script:
    - echo "Déploiement en production - Phase EFREI"
    - echo "Images disponibles dans le registry GitLab"
    - echo "Frontend - $CI_REGISTRY_IMAGE/frontend:latest"
    - echo "Backend Node - $CI_REGISTRY_IMAGE/backend-node:latest"
    - echo "Backend .NET - $CI_REGISTRY_IMAGE/backend-dotnet:latest"
    - echo "Backend Spring Boot - $CI_REGISTRY_IMAGE/backend-springboot:latest"
  when: manual
  only:
    - main
